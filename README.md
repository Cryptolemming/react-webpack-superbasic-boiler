# Basic React / Webpack / Node / Express boiler

clone
```
git clone
```

install repositories
```
npm install
```

run (use nodemon for continuous)
```
node app.js 
``

to build (--watch flag for continuous)
```
webpack --watch
```

